# Grading Server (Python)

- Dynamically loads the testing file,
  - generally saved to this
  - if not loads from the s3 bucket
- Dynamically loads the submission file
  - downloaded and loaded from s3
  - deleted afte submission
